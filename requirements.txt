# tools/ 脚本依赖　安装：pip install -r requirements.txt

# ---- md2html.py ----
markdown>=3.5

# ---- video2md.py ----
faster-whisper>=1.0          # 语音转写(CPU int8)
rapidocr-onnxruntime>=1.3    # 画面 OCR
imageio-ffmpeg>=0.4.9        # 自带静态 ffmpeg（bili_dl.py 也用）
pillow>=10.0
numpy>=1.26
requests>=2.31               # DeepSeek API（bili_dl.py 也用）
opencc-python-reimplemented  # 繁->简兜底（Whisper 偶发输出繁体）

# ---- 口播工程配音（可选）----
gTTS>=2.5                    # 占位配音
mutagen>=1.47                # 音频时长测量
# yt-dlp                     # 通用视频下载（B站被412时用 tools/bili_dl.py）
